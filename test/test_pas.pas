program zaher;

uses
  SysUtils;
var
  s: string;
  x: integer;
begin
  x := 10;
  writeln(x);
  x:=x + 10;
  writeln(x);
 sss  s := 'Hi World';
  writeln(s);
  writeln('Bye');
  writeln('Press Enter to exit');
  ReadLn();
end.