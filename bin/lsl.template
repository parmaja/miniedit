[state_entry | state_entry()]
state_entry()
{
    |;
}

[changed | changed(integer change)]
 changed(integer change)
 {
     |
 }

[llLinkParticleSystem | Defines a particle system for the containing prim based on a list of rules.]
llLinkParticleSystem(link|, rules)
